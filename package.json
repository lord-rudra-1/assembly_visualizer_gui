{
  "name": "arm-assembly-visualizer",
  "version": "1.0.0",
  "description": "A GUI for visualizing ARM assembly code execution",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "make",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 